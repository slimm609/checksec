name: checksec-release
on:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Run tests with coverage
        run: go test ./pkg/... -count=1 -coverpkg=./pkg/... -covermode=count -coverprofile=coverage.out

      - name: Enforce coverage threshold
        run: |
          pct=$(go tool cover -func=coverage.out | awk '/^total:/ {print substr($3, 1, length($3)-1)}')
          echo "Total pkg coverage: ${pct}%"
          awk -v p="$pct" 'BEGIN { exit (p+0 < 80.0) }' || { echo "Coverage ${pct}% is below the 80% floor"; exit 1; }

      - name: Build checksec binary for Docker context
        run: go build -o checksec .

      - name: Docker build
        run: docker build -t slimm609/checksec:latest .

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Push to Docker Hub
        uses: docker/build-push-action@v6
        with:
          push: true
          tags: slimm609/checksec:latest
